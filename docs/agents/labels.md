# Workflow labels

## Agent-ready work

実装エージェントが開始できる作業には、次の label を使用する。

```text
ready-for-agent
```

現在の GitHub labels に同等の label はない。label の作成や付与は GitHub への変更を伴うため、
Issue 公開と同様に明示的な承認後だけ行う。

追加の triage state は初期 workflow の対象外とし、必要になった時点で別途設計する。
